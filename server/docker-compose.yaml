services:
  battleship:
    build: .
    container_name: battleship
    restart: unless-stopped
    ports:
      - "1990:1990"
    environment:
      PORT: "1990"
      DB_PATH: /data/battleship.db
    volumes:
      - battleship_data:/data

volumes:
  battleship_data:
